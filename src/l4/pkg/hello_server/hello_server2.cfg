-- vim:set ft=lua:
require("L4");

local ldr = L4.default_loader;
local factory = L4.default_loader:new_channel();

L4.default_loader:start( {
  caps = { channel = factory:svr() },
  log = { "server", "blue" }
}, "rom/server");

L4.default_loader:start( {
  caps = { channel = factory:create(0, "some string") },
  log = { "client", "green" }
}, "rom/client");
